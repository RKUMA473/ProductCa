server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://guthub.com/shabbirkaderi120/config-server.git
          clone-on-start: true